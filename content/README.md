# Pelican site backup

This directory contains the original Pelican copies of the input files.

It remains for now, to allow re-running the Pelican -> Quarto port scripts.

The current output files generating the live content are in `posts/`

I will remove `content/` in due course.
